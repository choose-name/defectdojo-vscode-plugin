/**
 * Finding details panel
 * 
 * Note: The FindingDetailsPanel class lives in extension.ts because of the large HTML payload.
 * HTML can be moved into separate files or templated in the future.
 */

// Re-export from extension.ts
export { FindingDetailsPanel } from '../extension';
