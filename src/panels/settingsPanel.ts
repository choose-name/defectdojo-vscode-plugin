/**
 * DefectDojo settings panel
 * 
 * Note: The SettingsPanel class lives in extension.ts due to the large HTML payload.
 * HTML can be moved into separate files or templated in the future.
 */

// Re-export from extension.ts
export { SettingsPanel } from '../extension';
